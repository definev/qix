import 'package:flutter/material.dart';

class DebugColors {
  static const boundary = Colors.white;
  static const filledArea = Colors.teal;
  static const ballLine = Colors.amber;
  static const ball = Colors.blue;
}
